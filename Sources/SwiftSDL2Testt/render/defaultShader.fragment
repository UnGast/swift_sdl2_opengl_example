#version 330 core

uniform vec4 color;
uniform sampler2D inTexture;

in vec4 fragColor;
in vec2 TexCoord;

out vec4 FragColor;

void main()
{
    FragColor = texture(inTexture, TexCoord); // vec4(1.0f, 0.5f, 0.2f, 1.0f);
}